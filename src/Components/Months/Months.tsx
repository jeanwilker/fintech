import Button from '../Button/Button';

const Months = () => {
  return (
    <div className="flex gap-5">
      <Button n={-2} />
      <Button n={-1} />
      <Button n={0} />
    </div>
  );
};

export default Months;
